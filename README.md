# hochzeit.axelquack.de

Password-protected wedding site. Ask Ulrike or Axel for the passphrase.

This repository only holds the **encrypted** GitHub Pages artifact (`gh-pages`).
The editable source lives in the private repo `axelquack/hochzeit`.
